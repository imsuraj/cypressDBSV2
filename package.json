{
  "name": "cypress-dbsv2",
  "version": "1.0.0",
  "description": "THis is a cypress dbsv2",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "delete:reports": "rm -r cypress/reports/* || true",
    "prereport": "npm run delete:reports",
    "mochawesome:merge": "npx mochawesome-merge \"cypress/reports/mochawesome/*.json\" > mochawesome.json && npx marge mochawesome.json",
    "junit:merge": "npx junit-merge -d cypress/reports/junit -o cypress/reports/junit/result.xml",
    "cypress:e2e": "npx cypress run",
    "cy:open_qa": "npx cypress open --env username=abc,password=ancd ",
    "cy:open_process": "npx cypress open --env username=$DBS_USERNAME,password=$DBS_PASSWORD",
    "cy:run_process": "npm run prereport && cypress run --browser chrome --env username=$DBS_USERNAME,password=$DBS_PASSWORD  --spec 'cypress/e2e/tests/Login/loginTest.cy.js,cypress/e2e/tests/SalesInvoice/salesInvoiceTest.cy.js,cypress/e2e/tests/Configuration/Others/Promotion/*.cy.js'",
    "cy:run_processWithReport": "npm run prereport && cypress run --env username=$DBS_USERNAME,password=$DBS_PASSWORD,baseUrl=$DBS_URL --browser chrome --spec 'cypress/e2e/ßtests/SalesInvoice/salesInvoiceTest.cy.js';npm run junit:merge; npm run mochawesome:merge"
  },
  "author": "Suraj",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "cypress": "^13.6.4",
    "cypress-mochawesome-reporter": "^3.5.0",
    "cypress-multi-reporters": "^1.6.3",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "faker": "^5.5.3"
  }
}
